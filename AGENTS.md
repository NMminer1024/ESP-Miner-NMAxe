# Repo Working Rules

## Baseline Commit Rule

- After every completed code change, create a git baseline commit before moving on.
- Commit message language defaults to the language already used by recent project history.
- If the user explicitly asks for a specific commit language, follow the user's choice.
- This rule has highest priority for normal repository work unless the user explicitly says not to commit yet.

